/**
 * CollaborationPanel - DISABLED
 * Prevents collaboration UI from causing errors
 */

import React from 'react';

const CollaborationPanel: React.FC = () => {
  // Component disabled to prevent WebSocket errors
  return null;
};

export default CollaborationPanel;
